# ROADMAP

按里程碑整体推进，不按微任务拆分。每个里程碑完成以「能在浏览器中完整演示」为准。

## M1 端到端演示闭环（当前优先）

目标：用一套样例数据，把核心闭环从头到尾真实走通。

- 样例数据走通：预测导入 → 排班导入 → 对比计算 → 复核工单
- Dashboard 接入真实数据，替换静态 mock
- 清理「只读解释页 / 禁用占位」类遗留界面，换成可操作功能或移除

## M2 排班能力（核心价值补位）

目标：系统自身能生成排班，而不只是接收外部排班。

- 班次模板与排班规则配置
- 自动 / 建议排班算法 MVP：按需求预测生成排班草案，支持人工调整后发布

## M3 生产化

目标：从本地原型走向可交付使用。

- 真实 Excel 导入（替代纯 CSV）
- 结果导出
- 登录认证与基础角色（规划员 / 管理者）
