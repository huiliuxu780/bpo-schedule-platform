# ROADMAP

按里程碑整体推进，不按微任务拆分。每个里程碑完成以「能在浏览器中完整演示」为准。

## M1 端到端演示闭环（当前优先）

目标：用一套样例数据，把核心闭环从头到尾真实走通。

完成判定标准：

- 预测 → 排班导入 → 对比 → 复核全程无假数据（含删除 lib/ 中的静默 fallback 降级）
- 从首页出发不依赖深链可走完全闭环
- 占位页 / 解释页清理完毕

## M2 排班工作台（MVP-A）

目标：系统内完成人工排班并发布。

完成判定标准：

- 基础配置四标签可用（员工与技能 / 班次与活动 / 规则 / 状态映射）
- 排班计划台矩阵编辑 + 半小时覆盖计算 + 校验 + 发布走通
- 顶部导航 3 项上线、旧路由重定向下线

## M3 执行核验（MVP-B）

目标：计划执行对比与异常处理。

完成判定标准：实际执行页走通「状态日志导入 → 四类异常（迟到 / 早退 / 缺勤 / 小休超时）→ 确认 / 忽略 / 解决 → 详情抽屉」。

## M4 生产化

目标：从本地原型走向可交付使用。

- 真实 Excel 导入（替代纯 CSV）
- 结果导出
- 登录认证与基础角色（规划员 / 管理者）

## 后续能力

- 排班建议算法（仅"生成草稿、人工确认发布"形态）
- 需求预测生成
- 实时遵时

注：原 M2 的"自动排班算法 MVP"已后移至后续能力（先可靠人工排班）。
