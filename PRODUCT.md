# PRODUCT.md — 产品北极星

## 一句话定义

面向 BPO（客服外包）交付团队的人力计划与履约管理平台。

## 目标用户

- **排班规划员**：根据人力需求编制班表，盯覆盖率与人力缺口
- **交付管理者**：查看履约偏差，推动异常处理

## 核心业务闭环（一切功能围绕它）

```
基础配置
  → 导入标准人力需求（半小时粒度）
  → 编制并发布班表
  → 导入实际状态日志
  → 计划实际对比
  → 异常复核
```

第一阶段需求完全来自外部导入，需求预测生成属于后续能力；现有"需求预测"链路按需求导入的一种形态对待。

## 现状盘点（2026-08）

**已建成**：

- 数据导入中心：CSV 上传、字段映射模板、批次/版本管理、失败行修正
- 主数据：坐席、组织、职场、供应商、技能、技能服务组的完整 CRUD
- 三类数据导入（需求/排班/日志）与应用链路、版本台账
- 对比计算：预测 vs 排班、排班 vs 实际的计算与结果查询
- 复核工单：证据、结论、关单闭环

**薄弱 / 缺失**：

- 排班完全依赖外部导入，系统自身没有排班生成能力（最大价值缺口）
- 只支持 CSV，不支持真实 Excel 导入
- 无登录认证、无导出

## 信息架构裁决

- 默认首页为排班计划台，Dashboard 下线
- 一级导航为顶部 3 项：排班计划台 / 实际执行 / 基础配置
- 不可用记录并入基础配置的员工排班限制；排班风险、班次明细降级为排班计划台内视图
- 导入记录、版本、校验、发布、复核入口按任务内承载（弹窗/抽屉/面板），不作为独立导航页面

## 产品判断三原则

1. **闭环优先**：能让核心闭环多走一步的功能，优先级永远高于单点增强。
2. **角色-场景必答**：每个新页面必须能回答「哪个角色、在闭环哪一步、用它做什么」。
3. **功能要"能做某事"**：不做只解释"某事为什么不能做"的页面或占位。

## 暂不做

审批流、结算计费、多租户权限、外部系统集成（CORN 等）；需求预测生成、自动排班建议、实时遵时（均属后续能力）。
