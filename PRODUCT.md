# PRODUCT.md — 产品北极星

## 一句话定义

面向 BPO（客服外包）交付团队的人力计划与履约管理平台。

## 目标用户

- **排班规划员**：根据需求预测安排班次，盯覆盖率与人力缺口
- **交付管理者**：查看履约偏差，推动异常处理

## 核心业务闭环（一切功能围绕它）

```
需求预测（每个半小时时段需要多少坐席）
  → 排班（谁在哪个班次上岗）
  → 履约（实际登录 / 状态日志）
  → 对比（预测 vs 排班、排班 vs 实际）
  → 复核（缺口 / 迟到等异常工单处理）
```

## 现状盘点（2026-08）

**已建成**：

- 数据导入中心：CSV 上传、字段映射模板、批次/版本管理、失败行修正
- 主数据：坐席、组织、职场、供应商、技能、技能服务组的完整 CRUD
- 生产版本：需求预测、人员排班、登录/状态日志的导入应用与版本台账
- 对比计算：预测 vs 排班、排班 vs 实际的计算与结果查询
- 复核工单：证据、结论、关单闭环
- Dashboard 骨架（shadcn/ui 风格，暗/亮主题）

**薄弱 / 缺失**：

- 排班完全依赖外部导入，系统自身没有排班生成能力（最大价值缺口）
- 只支持 CSV，不支持真实 Excel 导入
- Dashboard 是静态 mock，未接真实数据
- 无登录认证、无导出

## 产品判断三原则

1. **闭环优先**：能让核心闭环多走一步的功能，优先级永远高于单点增强。
2. **角色-场景必答**：每个新页面必须能回答「哪个角色、在闭环哪一步、用它做什么」。
3. **功能要"能做某事"**：不做只解释"某事为什么不能做"的页面或占位。

## 暂不做

审批流、结算计费、多租户权限、外部系统集成（CORN 等）、自动排班之外的智能算法。
