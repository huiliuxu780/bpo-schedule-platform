version: 1
updated_at: "2026-06-04"
source_of_truth: "current_active_tasks"
notes:
  - "Only current executable tasks belong here."
  - "Each ready story must have a matching active task before execution."
  - "Done task history stays in legacy files or archive during the transition."
tasks:
  - id: IM111
    story_ids: [US731]
    requirement_ids:
      - R811
    title: "人员排班版本详情只读 API"
    status: ready
    priority: P1
    required_workflow: database-persistence
    branch: codex/im111-schedule-version-detail-api
    allowed_files:
      - "backend/app/main.py"
      - "backend/app/models.py"
      - "backend/app/personnel_schedule_persistence.py"
      - "backend/tests/test_personnel_schedule_production_api.py"
      - "backend/tests/test_personnel_schedule_persistence.py"
      - "docs/current/**"
      - "docs/registry/TRACE_INDEX.yaml"
      - "docs/PROJECT_STATE.md"
      - "docs/raw-requirements.md"
      - "docs/user-stories.md"
      - "docs/dev/branch-log.md"
      - "tasks/backlog.yaml"
    forbidden_files:
      - "package.json"
      - "package-lock.json"
      - "pnpm-lock.yaml"
      - "yarn.lock"
      - "backend/alembic/**"
      - "app/**"
      - "components/**"
    acceptance:
      - "提供按 batch_id 查询人员排班生产版本详情的只读 API。"
      - "响应包含来源批次、schedule_version_id、业务日期范围、排班明细和 0.5h 展开区间。"
      - "明细保留员工、职场、项目、技能、班次类型引用字段，便于后续前端解释引用校验。"
      - "`bash scripts/check-state.sh --strict`、后端目标测试、`git diff --check` 和 `bash scripts/check.sh` 通过。"
    auto_run: false
    pm_confirmation_required: true
    conflict_risk: high
    stop_conditions:
      - "需要新增依赖或修改 package/lockfile"
      - "需要新增 schema/migration 或生产持久化配置"
      - "范围扩展到前端接入、发布、冻结、取消发布、审批、导出、批量、权限、真实外部接口、自动排班、生产公式、结算或收费因子"
      - "验证失败"
    commit_after_done: true
