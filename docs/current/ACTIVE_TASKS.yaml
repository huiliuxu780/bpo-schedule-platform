version: 1
updated_at: "2026-06-04"
source_of_truth: "current_active_tasks"
notes:
  - "Only current executable tasks belong here."
  - "Each ready story must have a matching active task before execution."
  - "Done task history stays in legacy files or archive during the transition."
tasks:
  - id: IM101
    title: "人员排班发布冻结边界安全壳"
    story_ids: ["US721"]
    status: ready
    required_workflow: frontend-scaffold
    dependencies:
      - "IM100"
    allowed_files:
      - "app/schedule-plans/production/**"
      - "components/personnel-schedule-production-workbench.tsx"
      - "components/personnel-schedule-production-model.ts"
      - "scripts/tests/personnel-schedule-production-model.test.mjs"
      - "docs/current/**"
      - "docs/registry/TRACE_INDEX.yaml"
      - "docs/PROJECT_STATE.md"
      - "docs/raw-requirements.md"
      - "docs/user-stories.md"
      - "docs/dev/branch-log.md"
      - "tasks/backlog.yaml"
    forbidden_files:
      - "package.json"
      - "package-lock.json"
      - "pnpm-lock.yaml"
      - "yarn.lock"
      - "backend/alembic/**"
      - "backend/app/**"
      - "backend/tests/**"
    acceptance:
      - "发布、冻结或取消发布动作必须先展示来源版本、展开结果和引用校验边界。"
      - "动作按钮保持安全壳状态，不提交真实生产状态变化。"
      - "写入动作进入前需要单独确认，不默认扩展到后端/schema/migration。"
      - "不做审批、导出、批量、权限、真实外部接口、自动排班、生产公式、结算或收费因子。"
      - "`bash scripts/check-state.sh --strict`、前端模型测试、shadcn gate、页面 smoke、`git diff --check` 和 `bash scripts/check.sh` 通过。"
    auto_run: false
    pm_confirmation_required: true
    conflict_risk: high
    stop_conditions:
      - "需要新增依赖或修改 package/lockfile"
      - "范围扩展到后端、schema、migration 或数据库结构变更"
      - "范围扩展到审批、导出、权限、批量、真实外部接口、自动排班、生产公式、结算或收费因子"
      - "验证失败"
    commit_after_done: true
