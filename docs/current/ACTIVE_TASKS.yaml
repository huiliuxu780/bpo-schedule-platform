version: 1
updated_at: "2026-07-06"
source_of_truth: "current_active_tasks"
notes:
  - "Only current executable tasks belong here."
  - "Each ready story must have a matching active task before execution."
  - "Done task history stays in legacy files or archive during the transition."
tasks:
  - id: IM296
    title: "排班师月班表发布工作台 v1"
    status: ready
    required_workflow: backend-vertical
    story_ids:
      - US884
    requirement_ids:
      - R964
    branch: "codex/im296-roster-publish-workbench"
    base_branch: "codex/im295-roster-persistence"
    product_result: "排班师可在 `/roster-drafts?month=2026-08` 完成当前草稿发布，并在同一工作台读回 current published snapshot。"
    scope:
      do:
        - "Add narrow local API endpoints for publish current roster draft, read current published snapshot, acquire/check/release edit lock."
        - "Wire `/roster-drafts` to call the API for publish and snapshot readback."
        - "Use the current generated/local-edited 2026-08 draft sample as the acceptance fixture."
        - "Display hard blockers, non-blocking soft risks, shift counts, half-hour Arranged coverage, and diff summary in the publish preview/snapshot drawer."
        - "After publish, make the current version read-only and default the workbench to the published snapshot."
        - "Add focused backend, frontend/model, and browser acceptance coverage."
      do_not:
        - "Do not split this into API-only, UI-only, persistence-only, or lock-only tasks."
        - "Do not add scheduled publish UI, full revision draft editing, Forecast vs published persistence gap, forecast model, standard-capacity model, Excel import, auth, permissions, approval, notification, export, batch, automatic scheduling, production formulas, settlement, or charge-factor work."
        - "Do not add dependencies or modify package/lockfiles."
        - "Do not create new migrations or broaden the IM295 persistence schema unless a blocker is reported and PM confirms a new database task."
    allowed_files:
      - "backend/app/main.py"
      - "backend/app/roster_drafts.py"
      - "backend/app/roster_service.py"
      - "backend/app/roster_persistence.py"
      - "backend/tests/test_roster_publish_api.py"
      - "backend/tests/test_roster_service.py"
      - "app/roster-drafts/page.tsx"
      - "components/roster-draft-workbench.tsx"
      - "lib/roster-drafts.ts"
      - "lib/roster-draft-fixtures.ts"
      - "scripts/tests/roster-draft-generation-model.test.mjs"
      - "scripts/tests/roster-draft-workbench-structure.test.mjs"
      - "docs/current/**"
      - "docs/registry/**"
      - "docs/task-log.md"
      - "docs/audit-report.md"
      - "docs/dev/branch-log.md"
      - "tasks/backlog.yaml"
      - "docs/raw-requirements.md"
      - "docs/user-stories.md"
    forbidden_files:
      - "package.json"
      - "package-lock.json"
      - "pnpm-lock.yaml"
      - "yarn.lock"
      - "backend/migrations/**"
      - "project-progress/**"
      - "/Users/mac/Documents/Codex/01_Projects/bpo-schedule-platform-lab/**"
    acceptance:
      - "`/roster-drafts?month=2026-08` can publish the current generated/local-edited draft through local API and read back current published snapshot."
      - "Locked-by-other state makes the workbench read-only and blocks publish; current actor can release own lock."
      - "Hard publish errors block with visible reasons; soft risks are recorded and visible in preview/snapshot."
      - "Published snapshot shows solidified shift counts, half-hour Arranged coverage, soft risks, and diff summary."
      - "After publish, current draft cells are not editable and the workbench defaults to the published snapshot."
      - "Focused backend/frontend tests and manual browser smoke pass; final `bash scripts/check.sh` passes before commit."
