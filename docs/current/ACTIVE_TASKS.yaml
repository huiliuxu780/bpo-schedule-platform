version: 1
updated_at: "2026-06-04"
source_of_truth: "current_active_tasks"
notes:
  - "Only current executable tasks belong here."
  - "Each ready story must have a matching active task before execution."
  - "Done task history stays in legacy files or archive during the transition."
tasks:
  - id: IM103
    story_ids: [US723]
    requirement_ids:
      - R803
    title: "需求预测版本详情与对齐结果"
    status: ready
    priority: P1
    required_workflow: frontend-scaffold
    branch: codex/im103-demand-forecast-version-detail
    allowed_files:
      - "app/demand-plans/production/**"
      - "components/demand-forecast-production-workbench.tsx"
      - "components/demand-forecast-production-model.ts"
      - "scripts/tests/demand-forecast-production-model.test.mjs"
      - "docs/current/**"
      - "docs/registry/TRACE_INDEX.yaml"
      - "docs/PROJECT_STATE.md"
      - "docs/raw-requirements.md"
      - "docs/user-stories.md"
      - "docs/dev/branch-log.md"
      - "tasks/backlog.yaml"
    forbidden_files:
      - "package.json"
      - "package-lock.json"
      - "pnpm-lock.yaml"
      - "yarn.lock"
      - "backend/alembic/**"
      - "backend/app/**"
      - "backend/tests/**"
    acceptance:
      - "从需求预测生产工作台可进入预测版本详情。"
      - "详情页展示来源批次/版本、业务日范围、技能组、等级、时段粒度和对齐状态。"
      - "缺少预测明细或对齐结果时展示明确空态或阻塞态，不伪造技能组/等级/时段明细。"
      - "不新增后端 API、schema/migration、依赖、审批、导出、批量、权限、真实外部接口、自动排班、生产公式、结算或收费因子。"
      - "`bash scripts/check-state.sh --strict`、前端模型测试、shadcn gate、页面 smoke、`git diff --check` 和 `bash scripts/check.sh` 通过。"
    auto_run: true
    pm_confirmation_required: false
    conflict_risk: medium
    stop_conditions:
      - "需要新增依赖或修改 package/lockfile"
      - "范围扩展到后端、schema、migration 或数据库结构变更"
      - "范围扩展到审批、导出、权限、批量、真实外部接口、自动排班、生产公式、结算或收费因子"
      - "验证失败"
    commit_after_done: true
