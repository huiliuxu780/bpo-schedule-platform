version: 1
updated_at: "2026-07-08"
source_of_truth: "current_active_tasks"
notes:
  - "Only current executable tasks belong here."
  - "Each ready story must have a matching active task before execution."
  - "Done task history stays in legacy files or archive during the transition."
tasks:
  - id: IM308
    title: "班务变更申请月班表调整承接"
    status: ready
    required_workflow: frontend-scaffold
    story_ids:
      - US896
    requirement_ids:
      - R976
    branch: "codex/im308-duty-change-adjustment-handoff"
    base_branch: "codex/im305-roster-change-center"
    product_result: "排班师可在 `班务变更申请` 同页三栏里同意申请、调整当前格班次、保存为 `已调整`，并继续处理下一条。"
    scope:
      do:
        - "Reshape `/roster-change-governance` into the confirmed same-page three-column request adjustment workflow."
        - "Locate the selected request's current published roster cell in the middle adjustment area."
        - "Support current-cell quick adjustment through a fixed shift-code dropdown."
        - "Show lightweight impact hints before save."
        - "On save, reuse existing local roster revision/publish/request resolve APIs where possible so the adjusted request keeps a revision anchor."
        - "After save, return to the queue and select the next pending request."
        - "Add focused frontend structure coverage and browser smoke evidence."
      do_not:
        - "Do not implement swap two-person linkage, full roster editor behavior, complex hour/skill/coverage validation, automatic scheduling, forecasting, standard-capacity modeling, Excel import, approval, auth, permissions, notification, export, batch, production formulas, settlement, or charge-factor work."
        - "Do not add dependencies or modify package/lockfiles."
        - "Do not add database tables, migrations, or new persistence fields. Stop for PM confirmation if existing APIs cannot support the revision-anchor save path."
    allowed_files:
      - "components/roster-change-governance-workbench.tsx"
      - "scripts/tests/roster-change-governance-structure.test.mjs"
      - "docs/current/**"
      - "docs/registry/**"
      - "docs/task-log.md"
      - "docs/audit-report.md"
      - "docs/dev/branch-log.md"
      - "tasks/backlog.yaml"
      - "docs/raw-requirements.md"
      - "docs/user-stories.md"
      - "docs/superpowers/specs/2026-07-08-duty-change-adjustment-handoff-design.md"
    forbidden_files:
      - "package.json"
      - "package-lock.json"
      - "pnpm-lock.yaml"
      - "yarn.lock"
      - "backend/migrations/**"
      - "backend/app/roster_persistence.py"
      - "project-progress/**"
      - "/Users/mac/Documents/Codex/01_Projects/bpo-schedule-platform-lab/**"
    acceptance:
      - "`/roster-change-governance?month=2026-08` renders same-page three-column adjustment workflow."
      - "A pending request can be agreed, adjusted through fixed shift dropdown, and saved as `已调整`."
      - "The saved adjusted request has `result_type=adjusted` and a non-empty `linked_revision_version_id`."
      - "The queue selects the next pending request after save."
      - "Focused frontend tests, browser smoke, and final `bash scripts/check.sh` pass before commit."
