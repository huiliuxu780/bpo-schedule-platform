version: 1
updated_at: "2026-06-03"
source_of_truth: "current_active_tasks"
notes:
  - "Only current executable tasks belong here."
  - "Each ready story must have a matching active task before execution."
  - "Done task history stays in legacy files or archive during the transition."
tasks:
  - id: IM086
    title: "版本结果页本地比对计算受控入口"
    story_ids: ["US706"]
    status: ready
    required_workflow: frontend-scaffold
    dependencies:
      - "IM085"
    allowed_files:
      - "app/data-quality/actions.ts"
      - "app/data-quality/comparison-runs/**"
      - "app/data-quality/review-cases/**"
      - "components/import-center-comparison-run-detail-workspace.tsx"
      - "components/import-center-result-trace-panel.tsx"
      - "components/import-center-model.ts"
      - "scripts/tests/import-center-model.test.mjs"
      - "docs/current/**"
      - "docs/registry/TRACE_INDEX.yaml"
      - "docs/PROJECT_STATE.md"
      - "docs/raw-requirements.md"
      - "docs/user-stories.md"
      - "docs/dev/branch-log.md"
      - "tasks/backlog.yaml"
    forbidden_files:
      - "package.json"
      - "package-lock.json"
      - "pnpm-lock.yaml"
      - "yarn.lock"
      - "backend/alembic/**"
      - "backend/app/**"
      - "backend/tests/**"
    acceptance:
      - "版本结果页只在来源版本和 comparison_type 足够明确时展示受控 `发起本地比对` 入口。"
      - "提交后在当前版本结果语境展示成功/失败反馈，并提供进入新 comparison run 详情或结果列表的入口。"
      - "来源版本缺失、类型不支持或上下文不足时只展示阻塞原因，不展示写入按钮。"
      - "复用现有 comparison calculate API 和结果查询能力，不新增后端 API、schema/migration、依赖、审批、导出、批量、权限、真实外部接口、生产公式、结算、自动排班或收费因子。"
      - "`bash scripts/check-state.sh --strict`、前端模型测试、shadcn gate、页面 smoke、`git diff --check` 和 `bash scripts/check.sh` 通过。"
    auto_run: true
    pm_confirmation_required: false
    conflict_risk: medium
    stop_conditions:
      - "需要新增依赖或修改 package/lockfile"
      - "范围扩展到后端、schema、migration 或数据库结构变更"
      - "范围扩展到审批、导出、权限、批量、真实外部接口、生产公式、结算或收费因子"
      - "验证失败"
    commit_after_done: true
